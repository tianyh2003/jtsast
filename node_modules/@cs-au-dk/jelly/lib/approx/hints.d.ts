import { ReadHint, HintsJSON, WriteHint, RequireHint, EvalHint } from "../typings/hints";
import { LocationJSON } from "../misc/util";
export declare class Hints {
    readonly modules: Array<string>;
    readonly moduleIndex: Map<string, number>;
    readonly functions: Set<string>;
    readonly reads: Map<string, ReadHint[]>;
    readonly writes: Map<string, WriteHint[]>;
    readonly requires: Map<string, RequireHint[]>;
    readonly evals: Map<string, EvalHint[]>;
    addModule(m: string): number;
    addFunction(f: LocationJSON): void;
    addReadHint(h: ReadHint): void;
    addWriteHint(h: WriteHint): void;
    addRequireHint(h: RequireHint): void;
    addEvalHint(h: EvalHint): void;
    toJSON(): HintsJSON;
    clearHints(): void;
}
